// Fixed for now — no per-school settings mechanism exists in this codebase
// yet (see master doc's Book/BookLoan entities, neither has a rate field).
export const LOAN_PERIOD_DAYS = 14;
export const FINE_PER_DAY = 0.5;
export const MAX_ACTIVE_LOANS_PER_STUDENT = 5;
