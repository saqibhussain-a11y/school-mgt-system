DATABASE_URL="postgresql://sms:sms_dev_password@localhost:5432/sms_dev"

# Max Postgres connections this process's pool will open. Tune to your
# Postgres plan's own max_connections, especially if running more than one
# instance of the API. Defaults to 10 if unset.
# DATABASE_POOL_MAX="10"
